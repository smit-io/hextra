---
title: カラーパレット
weight: 3
---

アップストリームの Hextra は背景をフラットに塗ります。ライトモードは `bg-white`、ダークモードは単一の `#111` で、それ以外には Tailwind の青みがかった `gray` と `slate` を使います。本フォークでは、すべての背景・ボーダー・テキストに **Tailwind の `neutral`** という無彩色の 1 系統だけを使います。

<!--more-->

## 1 つの系統で両モードを賄う

テーマ内のニュートラルはすべて `neutral-*` ユーティリティです。ライトモードでは低い段階、ダークモードでは高い段階を使い、要素上でペアにします。

```html
<div class="hx:bg-neutral-100 hx:dark:bg-neutral-900">…</div>
```

`neutral` は `gray` や `slate` と違い、青みを持たない真の無彩色です。これが最も効くのは本文テキストです。読者がページに滞在している間ずっと目に入るため、暖色系のアクセントと寒色系のグレーの組み合わせは、はっきりした違和感がなくてもちぐはぐに見えます。

## 段階

テーマは各面を決まった段階に固定し、ページに対する位置関係が常に予測できるようにしています。

| 役割                                  | ライト        | ダーク        |
| ------------------------------------- | ------------- | ------------- |
| ページ — `--hextra-bg`                | `#f7f7f7`     | `#111111`     |
| 浮いた面 — コードブロック、カード     | `neutral-50`  | `neutral-950` |
| パネル — 折りたたみ、シリーズボックス | `neutral-50`  | `neutral-900` |
| オーバーレイ — ドロップダウン、メニュー | `neutral-100` | `neutral-900` |
| クローム — ファイル名バー、ホバー     | `neutral-200` | `neutral-800` |
| ボーダー                              | `neutral-400` | `neutral-800` |

方向がモードで反転する点に注意してください。ライトモードでは浮いた面はページより _明るく_、ダークモードでは _暗く_ なります。

ページ自体は例外です。`neutral` の段階ではなく専用トークン `--hextra-bg` で、その 2 つの値は意図的に Tailwind の段階の中間に置かれています。ページを延長するもの — ナビゲーションバーのぼかし、サイドバーのドロワー、固定フッター — が `dark:` の対を持たない `hx:bg-hextra-bg` だけを使うのもこのためです。トークン自体が `.dark` で切り替わります。

## カスタマイズ

Tailwind v4 は自身のテーマカラーを CSS カスタムプロパティとして公開しているため、シェードの上書きは 1 行で済みます。テーマ本体のスタイルシートの後に読み込まれる `assets/css/custom.css` に書いてください。

重要な名前は 2 つです。ページは `--hextra-bg` で、ライト用に一度、`.dark` の下でもう一度指定します。それ以外の面はすべて Tailwind の段階で、Hextra は Tailwind を `@import "tailwindcss" prefix(hx)` で読み込むため、出力されるカスタムプロパティにも接頭辞が付きます。つまり上書きすべき名前は `--color-neutral-50` ではなく `--hx-color-neutral-50` です。（アクセントだけは接頭辞が付きません。`styles.css` が `--color-accent-color-*` を手で定義して橋渡ししているためです。[アクセントカラー](accent-color)を参照してください。）

```css {filename="assets/css/custom.css"}
:root {
  /* 少し暖かいページ背景 */
  --hextra-bg: oklch(97.3% 0.005 85);

  /* その 1 段階上の浮いた面 */
  --hx-color-neutral-50: oklch(98.5% 0.002 85);
}

.dark {
  /* わずかに青みのあるダークページ */
  --hextra-bg: oklch(17.6% 0.01 250);
}
```

変更したいシェードだけを上書きすれば十分です。残りは Tailwind のデフォルトのままになります。

{{< callout type="warning" >}}
ランプの順序は保ってください。デフォルトと同じように、各シェードが隣より明るいか暗いかを維持します。コンポーネントはランプが単調であることを前提にしているため、順序を逆にするとボーダーが見えなくなったりテキストのコントラストが落ちたりします。
{{< /callout >}}

## なぜ独自ランプではなく `neutral` なのか

以前のフォークは `hextra-light-*`、`hextra-dark-*`、`hextra-white-*`、`hextra-black-*` という独自ランプ 4 系統を、`gray`・`slate`・`neutral` と並べて同梱していました。1 つの用途に 7 系統あり、どれを使うかの規則もなく、さらにモード別の 2 系統は番号の向きが逆でした（`hextra-light-100` はほぼ白、`hextra-dark-100` はほぼ黒）。

統合によって曖昧さがなくなり、同時に不具合も解消されました。ダークランプの `oklch` 明度値は変換ではなく `byte / 255` で算出されていたため、11 段階すべてが `#000000`–`#262626` の範囲に潰れ、最初の 4 段階は互いに 2 ポイント以内に収まっていました。`neutral` は全域にわたって十分な差を持ちます。

`hextra-accent-*` は影響を受けません。アクセントパレットは引き続きフォーク独自のものです。[アクセントカラー](accent-color)を参照してください。
